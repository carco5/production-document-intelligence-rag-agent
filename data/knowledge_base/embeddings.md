# Embeddings

Embeddings are dense numerical vector representations of text.

Their purpose is to map semantically similar texts to nearby points in vector space. This allows systems to compare meaning instead of relying only on exact keyword matching.

In RAG systems, embeddings are used to represent both document chunks and user queries for similarity search.
