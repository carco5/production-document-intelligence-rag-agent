# RAG Basics

Retrieval-Augmented Generation combines information retrieval with text generation.

Instead of asking a language model to answer only from its internal parameters, a RAG system first retrieves relevant external context and then uses that context to generate a grounded answer.

A typical RAG pipeline includes document loading, text chunking, embedding generation, vector search, context assembly, and final answer generation.
