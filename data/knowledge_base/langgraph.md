# LangGraph

LangGraph is a framework for building stateful LLM applications as graphs.

A workflow is represented using nodes and edges. Nodes perform actions such as retrieving documents or generating an answer. Edges define how execution moves through the graph.

LangGraph is useful when an LLM application needs memory, branching, retries, loops, or multi-step orchestration.
